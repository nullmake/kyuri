/**
 * @class ActionType
 * Enum-like class for action types within InputProcessor.
 */
class KeyActionType {
    /** @field {Integer} KEY - Action sends keys */
    static KEY => 1
    /** @field {Integer} FUNC - Action calls a function */
    static FUNC => 2
}
